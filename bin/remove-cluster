#!/bin/bash

set -e

echo "Removing service $APP_NAME"
kubectl delete services $APP_NAME

echo "Stopping container Cluster for $CONTAINER_CLUSTER"
gcloud container clusters delete $CONTAINER_CLUSTER --quiet
